export const STORAGE_KEYS = {
  language: 'app.language',
  theme: 'app.theme',
} as const;

export const DEFAULT_CURRENCY = 'INR';

export { RESPONSE_CODES } from './responseCodes';
export type { ResponseCode } from './responseCodes';
export { REGEX } from './regex';
